ingress:
  host: lecture-notes-staging
  funnel: true
restore:
  # History should open the way it does in production, recordings included.
  audio: true
app:
  # PARKED at 0 while recovering from the 2026-09-23 outage. This pod is the
  # heavy one, and bringing the cluster back up with it running risks the
  # same starvation before anything can be checked. Set to 1 once the node
  # has been idle and healthy for a few minutes — the thread cap in
  # values.yaml is the actual fix, this is just a careful restart.
  replicas: 0
  # The Mac that hosts the VM, from inside it (see charts/insidertrack/README.md).
  ollamaBaseUrl: http://host.lima.internal:11434
image:
  tag: main@sha256:79e5ba32372a2f2e4a9672cd2f6d5efe3bae4fe8cb4bb1443f50a682386b735a
  repository: ghcr.io/mrgutierrezmario/lecture-note-app
