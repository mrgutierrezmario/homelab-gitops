# Staging: the InsiderTrack release in the same namespace is the upstream,
# and its Ingress routes /mcp here — the MCP publishes nothing of its own,
# the way production's serve.json lays it out.
insidertrackUrl: http://insidertrack:8003

ingress:
  enabled: false
